---
description: Odoo requirement analysis, MCP model discovery, and PRD generation for Odoo 17, 18, or 19.
argument-hint: <17|18|19> [module_name]
---

Use the Skill tool to invoke the `odoo_commanding_system` skill, then load
`plan_analysis_workflow.md` from that same skill directory. Execute STEP 1
through STEP 11 of that workflow for Odoo version $1 (ask the user for the
version if $1 is not exactly one of 17, 18, or 19). If $2 is provided, use it
as the module name; otherwise ask the user for it before proceeding.
