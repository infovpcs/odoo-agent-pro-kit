# Claude Code Context

This workspace uses the [Odoo Agent Pro Kit](https://github.com/infovpcs/odoo-agent-pro-kit).
The full agent context — MCP quick start, slash-command routing, skills
reference — lives in `AGENTS.md` in this same directory. Read that file first.

If the `odoo-agent-pro-kit` plugin isn't installed yet:

```
/plugin marketplace add infovpcs/odoo-agent-pro-kit
/plugin install odoo-agent-pro-kit
```
